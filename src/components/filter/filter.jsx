import React from 'react';
import PropTypes from 'prop-types';
import './filter.css';

const Filter = () => (
  <div className="filter">
    Filter Component
  </div>
);

Filter.propTypes = {};

Filter.defaultProps = {};

export default Filter;
